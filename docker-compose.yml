services:
  backend:
    build: .
    ports:
      - "5000:5000"
    environment:
      DB_HOST: 172.31.2.124
      DB_USER: root
      DB_PASSWORD: root
      DB_NAME: employees
    container_name: be-container
    restart: unless-stopped
